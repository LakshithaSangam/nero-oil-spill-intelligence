"""Pydantic contracts shared across the pipeline and exposed to the frontend via OpenAPI.

Import from the submodules directly, e.g. ``from app.schemas.detection import SpillDetection``.
Modules communicate *only* through these types.
"""
