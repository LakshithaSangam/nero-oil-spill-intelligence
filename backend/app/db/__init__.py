"""Persistence layer — SQLAlchemy 2.0 (async) + GeoAlchemy2 over PostgreSQL/PostGIS.

Models are added alongside the milestone that first needs to persist something
(investigations in M6, vessel risk history in M7+). M0 ships the session wiring only.
"""
